import {Link} from 'react-router-dom';import {useCart} from '../context/CartContext';export default function ProductCard({p}){const {add}=useCart();return <article className="card"><Link to={`/products/${p.id}`}><img src={p.imageUrl} alt={p.name}/></Link><small>{p.brand} · {p.category}</small><h3><Link to={`/products/${p.id}`}>{p.name}</Link></h3><strong>${(p.discountPrice||p.price).toFixed(2)}</strong><button onClick={()=>add(p.id)}>Add to cart</button></article>}
